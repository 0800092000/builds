install:
  - appveyor DownloadFile https://github.com/docker/machine/releases/download/v0.14.0/docker-machine-Windows-i386.exe

build: off

test: off

artifacts:
  - path: 'docker-machine-Windows-i386.exe'
